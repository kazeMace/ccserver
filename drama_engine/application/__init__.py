"""Application services for Drama Engine.

This layer owns service-facing orchestration for catalog, authoring, inspection,
generation and admin playtest workflows. It may depend on ``core`` domain and
runtime abstractions, while ``core`` should not depend on this layer.
"""

__all__: list[str] = []
